# 2026 首届 openvela AI 硬件开发者大赛 — 创造钟 — ZMath 介绍文档

## 一、项目概述

**ZMath** 是一款基于 openvela 快应用框架开发、专为智能手表场景打造的数学思维训练应用。它把经典的口算练习与"24 点"益智游戏搬到了手腕上，让用户在碎片化时间里随时随地锻炼运算能力与数学思维，是一款"轻、快、好玩"的随身数学训练工具。

应用包含四个页面：**首页（功能入口与积分展示）**、**24 点（四则运算挑战）**、**口算训练（题库选择）**、**口算测验（答题闯关）**，整体采用深色科技风 UI，针对手表小屏交互做了深度适配。

- 代码仓库：https://github.com/open-vela/contest2026_116_chuangzaozhong

## 二、解决的问题

1. **儿童与青少年"手机成瘾"、屏幕时间过长**：ZMath 将数学训练从手机转移到手表，让用户无需解锁手机即可训练，减少对手机娱乐内容的接触，把碎片时间转化为学习时间。
2. **传统口算练习枯燥、缺乏即时反馈**：纸质口算题卡无法自动判分、无法统计进步。ZMath 提供即时对错反馈、积分激励与正确率统计，形成"练习—反馈—激励"的闭环，显著提升练习动力。
3. **便携数学训练工具缺失**：传统数学 App 功能臃肿、依赖大屏。ZMath 轻量化设计，随抬随练，完美匹配手表这一"随身设备"的定位。
4. **24 点传统玩法门槛高**：纸牌 24 点需要洗牌、摆牌、人工验算。ZMath 一键随机出题、自动校验结果、即时积分，把复杂规则封装为极简交互。

## 三、目标受众与应用场景

### 目标受众

| 受众群体 | 需求特征 |
| --- | --- |
| 小学生及家长 | 需要趣味化、碎片化的口算/方程训练工具，家长可放心让孩子使用 |
| 初高中生 | 希望利用排队、课间、通勤等碎片时间锻炼心算与数感 |
| 数学爱好者 | 对 24 点等益智挑战有浓厚兴趣，追求思维训练与积分成就感 |
| 智能手表用户 | 希望手表不只有消息提醒，还能承载实用工具类应用 |

### 应用场景

- **课间 / 通勤 / 排队等碎片时间**：抬手即可进行一轮口算或一局 24 点，每次仅需 1-3 分钟；
- **家庭亲子互动**：家长与孩子轮流挑战 24 点，比速度、比解法，增进亲子互动；
- **课堂辅助练习**：作为数学课的趣味练习环节，快速生成题目并自动判分；
- **自律打卡**：通过积分体系培养每日数学训练习惯，形成正向激励。

## 四、openvela 系统能力的应用

本项目深度应用了 openvela 快应用框架提供的系统能力，实现轻量、高效的端侧体验：

1. **快应用框架（.ux 单文件组件 + MVVM 数据绑定）**：采用 `<template>` / `<script>` / `<style>` 三段式单文件组件，配合 `{{}}` 数据绑定与 `for`、`if`、`block` 模板指令实现声明式 UI 渲染，代码结构清晰、性能出色。
2. **system.router 页面路由**：通过 `router.push` 实现页面跳转与参数传递（如题库 ID `bankId` 传入测验页），`router.back` 支持随时返回，构建了"首页 → 训练 → 测验"的完整导航链路。
3. **system.storage 本地持久化**：使用 `storage.get` / `storage.set` 实现积分数据的本地持久化存储，用户退出应用后积分不丢失，支撑跨页面积分展示与累计激励机制。
4. **手表设备定向适配**：`manifest.json` 中声明 `deviceTypeList: ["watch"]`，按手表平台特性定向打包；`designWidth: 480` 按手表屏幕设计规范适配，保证小屏渲染质量。
5. **OLED 屏幕节能的深色主题**：全局采用 `#0f1923` 深色底 + 高饱和强调色的深色视觉体系，契合 OLED 手表屏低功耗特性，同时带来沉浸式的科技感体验。
6. **国际化资源目录（i18n）**：预留 `en`、`zh-CN` 多语言资源结构，为多语言发布提供扩展能力。

## 五、技术优化

1. **面向穿戴设备的小屏交互优化**：所有可点击区域均采用大尺寸触控目标（卡片式按钮、大号数字键），符合手表触控精度；页面层级扁平（最多两级跳转），减少操作路径。
2. **紧凑布局与极简状态管理**：每个页面在单文件内聚模板、逻辑与样式，页面私有状态（`private`）最小化；全局仅共享积分这一持久化状态，避免跨页状态同步开销。
3. **随机性与复玩性优化**：24 点每局随机生成 4 张 1-13 的数字牌；口算题库内置 40 道精选题目，每次测验通过 Fisher-Yates 洗牌算法随机打乱顺序，保证每次练习体验不同。
4. **可靠的算式校验机制**：24 点采用"公式构建 → 正则白名单校验 → 表达式求值"三段式校验，先以正则过滤非法字符，再计算并与 24 做浮点容差比较，兼顾安全性（防止注入）与判分准确度。
5. **异步存储读写与容错**：积分读写全部通过异步回调实现，并对读写失败做了兜底处理（失败时按 0 分或初始值处理），保证低端设备上也不阻塞 UI 渲染。
6. **动画与视觉性能**：进度条、反馈弹层等交互以渐变与过渡实现，避免复杂动效；渐变背景、圆角卡片等视觉均采用原生样式能力，无额外图片资源，安装包极小、启动迅速。

## 六、改进建议

1. **增强 AI 能力，实现动态出题**：当前题库为静态预置题目，建议后续接入端侧 AI 模型按用户水平动态生成题目，实现千人千面的自适应难度（依据历史正确率自动升降难度）。
2. **接入更多系统能力**：可调用 `system.vibrator` 在答对/答错时提供振动反馈，接入语音播报（TTS）朗读题目，进一步提升手表场景的使用体验；利用传感器能力（如抬腕监测）实现训练提醒。
3. **增加进阶题型**：在解方程、四则运算基础上扩展分数运算、根号、找规律、图形推理等题型，覆盖更广的数学知识点。
4. **引入排行榜与多端同步**：将积分与成绩通过云服务同步，支持家庭排行榜、好友 PK，进一步强化激励机制。
5. **离线与弱网适配**：手表经常脱离手机单独使用，建议完善纯离线模式，并缓存学习数据，断网不中断训练。
6. **数据可视化**：以周/月维度统计练习时长、正确率曲线、薄弱知识点，帮助家长与孩子直观看到进步。

## 七、开发过程与 AI 使用情况

本项目采用 **AI 辅助开发为主**的研发模式：

- **AI 代码占比超 95%**：应用的全部页面逻辑、UI 样式与交互代码均由 AI 编写完成，人工仅做需求定义、结果验收与微调。
- **Token 消耗达 3 亿**：整个开发过程累计消耗约 3 亿 token，涵盖需求分析、代码生成、多轮迭代优化与缺陷修复，充分验证了"AI 主导 + 人类把关"的敏捷开发模式在智能硬件应用开发中的可行性。

这种开发方式大幅缩短了从创意到可运行产品的周期，也证明了 openvela 快应用框架对 AI 编程工具的良好亲和力——模板语法规范、系统能力接口清晰，AI 可以高效产出高质量代码。

## 八、总结

ZMath 以"数学思维训练"为切入点，充分发挥 openvela 快应用框架轻量、高效、系统能力完备的优势，在智能手表这一全新载体上实现了 24 点与口算训练两大核心功能。它解决了碎片化数学训练工具的缺失问题，面向学生、家长与手表用户提供了随时随地的思维锻炼体验。未来随着 AI 动态出题、系统能力深化应用与多端协同的推进，ZMath 有望成为 openvela 生态中一款有代表性的教育类标杆应用。
